{#
/**
 * @file
 *
 * Default product template.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - product: The rendered product fields.
 *   Use 'product' to print them all, or print a subset such as
 *   'product.title'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ product|without('title') }}
 *   @endcode
 * - product_entity: The product entity.
 * - product_url: The product URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  <div class="product product--teaser">
    <div class="product__images">
      {{ product.field_images.0 }}
    </div>
    <div class="product__info">
      <h1 class="product__title">{{ product.title.0 }}</h1>
      <div class="product__color">{{ product.variations }}</div>
      <div class="product__description">{{ product.field_description.0 }}</div>
      <div class="product__price">{{ product.variation_price }}</div>
    </div>
  </div>
</article>
